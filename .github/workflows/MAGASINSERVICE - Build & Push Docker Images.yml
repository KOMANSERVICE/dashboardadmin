name: MAGASINSERVICE - Build and Push Docker Images

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'
    paths:
      - 'Services/MagasinService/**'
  pull_request:
    types: [closed]
    branches: [main]
    paths:
      - 'Services/MagasinService/**'

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/reusable-docker-build.yml
    permissions:
      contents: read
      packages: write
    with:
      image_api: magasinservice
      image_sql: magasinservicesql
      dockerfile_api: Services/MagasinService/MagasinService.Api/Dockerfile
      dockerfile_sql: backendadminsql.Dockerfile
      ef_project: Services/MagasinService/MagasinService.Infrastructure
      ef_startup_project: Services/MagasinService/MagasinService.Api
    secrets:
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
